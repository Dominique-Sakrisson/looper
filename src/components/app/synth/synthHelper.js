//converts target.value of DOM node to playable string
const keyToKeyString = ((note, octave) => {
  console.log(note);
  console.log(octave);
  let keyString;
  //   const keyString = (note + octave).toUpperCase();
  return keyString; 
});

export default keyToKeyString;
